<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
    <title>DPS945 MO:VES backend usage</title>
    <style>
        .header{
            height: 50px;
            width: 100%;
            border-bottom: 1px black solid;
            position: fixed;
            top: 0px;
            background-color: white;
        }
        .wrapper{
            display: flex;
            width: 100%;
            align-items: stretch;
            margin-top: 50px;
            height: calc(100vh - 50px);
        }
        .sidebar{
            padding-top: 30px;
            width: 30vw;
            overflow-y: auto;
        }
        .sidebar h4 {
            padding-left: 5px;
        }
        .sidebar ul {
            list-style: none;
            padding-left: 20px;
        }
        .sidebar ul li {
            margin-bottom: 10px;
        }
        .content{
            left: 30vw;
            width: 70vw;
            overflow-y: auto;
        }
        
        .content a:before {
            content: "";
            display: block;
            padding-top: 0.5em;
            margin-top: -0.5em;
        }
        
        .tablerow th {width: 20%;}
        .tablerow td {width: 80%; word-wrap: break-word;}
        .table { table-layout: fixed;}
    </style>
</head>
<body>
    <div>
        <div class="header">
            <h4>DPS945 MO:VES Project backend usage manual</h4>
        </div>
        <div class="wrapper">
            <div class="sidebar">
                {{#each data}}
                    <h4>{{@key}}</h4>
                    <ul>
                        {{#each this}}
                            <li><a href="#{{name}}">{{name}}</a></li>
                        {{/each}}
                    </ul>
                    <hr>
                {{/each}}
            </div>
            <div class="content">
                {{#each data}}
                    <h1>{{@key}}</h1>
                    {{#each this}}
                    <h4><a name="{{name}}">{{name}}</a></h4>
                    <table class="table table-striped">
                        <tbody>
                            {{#each this as |value key|}}
                            <tr class="tablerow">
                                <th>{{key}}</th>
                                <td><pre>{{json value}}</pre></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                    <br><br><br>
                    {{/each}}
                    <hr>
                {{/each}}
            </div>
        </div>
    </div>
</body>
</html>